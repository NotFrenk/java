import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;

class test_calcolatrice {

	@Test
	void testSomma() {
		es_calcolatrice c = new es_calcolatrice();
		assertEquals(5, c.somma(3, 2));
	}

	@Test
	void testSottrazione() {
		es_calcolatrice c = new es_calcolatrice();
		assertEquals(3, c.sottrazione(5, 2));
	}

	@Test
	void testMoltiplicazione() {
		es_calcolatrice c = new es_calcolatrice();
		assertEquals(6, c.moltiplicazione(3, 2));
	}

	@Test
	void testDivisione() {
		es_calcolatrice c = new es_calcolatrice();
		assertEquals(5, c.divisione(10, 2));
	}

}
